import palette;
pen[] allPens=Wheel();
unitsize(1.0 pt);
pen[] allPens=Wheel();
pen[] myPens = new pen[0];
for(int i=0;i< 0;++i)
  {
   int iPen = round(i*allPens.length/0);
   myPens[i] = allPens[iPen];
  }
dotfactor=12;
