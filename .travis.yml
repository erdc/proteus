sudo: false
cache:
  directories:
  - .hashdist
language: python
env:
- TEST_PROFILE="test.proteus.yaml"
python:
- 2.7
before_install:
- make hashdist
- make stack
install:
- cp .travisprofile.yml stack/default.yaml
- ./hashdist/bin/hit status stack/default.yaml
- make profile
script:
- set -o pipefail
- make check
notifications:
email: false
