CC='' CXX='' ./configure LDFLAGS="-L${PROTEUS_PREFIX}/lib" LIBS="-lsz -lz" CFLAGS="-I${PROTEUS_PREFIX}/include" --with-szlib=${PROTEUS_PREFIX} --with-zlib=${PROTEUS_PREFIX} --enable-shared --enable-threadsafe --with-pthread --prefix=${PROTEUS_PREFIX}
