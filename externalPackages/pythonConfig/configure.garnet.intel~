CFLAGS="-I${HOME}/include" LDFLAGS="-L${HOME}/lib" ./configure --prefix=${PROTEUS_PREFIX} --with-threads
#CFLAGS="-I${HOME}/include" LDFLAGS="-L${HOME}/lib" ./configure --prefix=${PROTEUS_PREFIX} --with-threads CC="icc -gcc -DPROTEUS_FFI64_HACK"
