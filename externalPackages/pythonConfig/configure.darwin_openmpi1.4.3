./configure --prefix=${PROTEUS_PREFIX} --enable-framework=${PROTEUS_PREFIX}/Library/Frameworks --enable-toolbox-glue CC=${CC}  CXX=${CXX} CPPFLAGS="-I/${PROTEUS_PREFIX}/include" CXXFLAGS="-I${PROTEUS_PREFIX}/include" LD=${CC}  LDFLAGS="-L${PROTEUS_PREFIX}/lib"
